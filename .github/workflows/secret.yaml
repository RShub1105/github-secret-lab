name: Secret Workflow

on:
  push:

jobs:
  use-secret:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print secret template safely
        run: |
          echo 'The secret is \${{ secrets.SECRET }}'
